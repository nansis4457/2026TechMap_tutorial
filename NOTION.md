# 🐢 RoomDungeon — 코드 설명 (Phase 1)

> visionOS 룸 트래킹으로, 사용자가 들어간 물리적 방 안에 몹(거북이)을 띄우는 앱.
> 방을 나가면 몹이 사라지고, 다시 들어오면 다시 나타난다.

---

## 📌 한눈에 보기

- **주제**: `RoomTrackingProvider`로 "지금 있는 방"을 감지해 그 안에 몹을 스폰
- **목표(Phase 1)**: 아무 방에나 들어가면 그 방 안에 몹이 떠 있다
- **필수 장비**: Apple Vision Pro 실기기 (시뮬레이터는 룸 트래킹 미지원)
- **핵심 증거**: `contains()` 검증 + 나갈 때 despawn → 룸 트래킹이 실제로 동작한다는 화면상 증명

---

## 🔄 전체 흐름

이 앱은 "센서가 데이터를 밀어주면 받아서 화면을 바꾼다"는 원리로 동작한다. 매 순간 방을
물어보는 게 아니라, 방이 바뀔 때마다 오는 이벤트를 **구독**해서 반응한다.

```
ImmersiveSpace  →  ARKitSession  →  RoomTrackingProvider  →  RealityKit Entity
 (권한·무대)        (센서 런타임)      (방 데이터 스트림)         (몹 = 결과물)
```

| 계층 | 책임 | 담당 파일 |
|---|---|---|
| 1. 무대·권한 | 몰입 공간 + 월드 센싱 권한 | App / Info.plist |
| 2. 런타임 | 세션 실행, 룸 트래킹 구독 | Manager |
| 3. 데이터 스트림 | 방 업데이트 이벤트 수신 | Manager |
| 4. 방 정체성 | `isCurrentRoom`으로 현재 방 판별 | Manager |
| 5. 스폰 지점 | 후보 점 → `contains()` 검증 | Manager |
| 6. 생명주기 | 입장 시 spawn / 퇴장 시 despawn | Manager |

---

## 📂 파일별 설명

### `RoomDungeonApp.swift` — 앱의 뼈대

- 화면이 **둘**이다: 처음 뜨는 **평면 창**과, 룸 트래킹이 도는 **몰입 공간**.
- 몰입 공간은 앱 시작 시 자동으로 못 열고 사용자가 직접 들어가야 해서 둘로 나눈다.
- 두 화면이 **같은 매니저**를 공유하도록 앱에서 한 번 만들어 environment로 주입한다.
- 몰입 스타일은 `.mixed` → 패스스루로 **실제 방이 보여야** 몹이 "내 방 안에" 떠 보인다.

### `ContentView.swift` — 입장 화면

- 제목 + 상태 문구 + 입장/퇴장 버튼.
- 상태 문구는 매니저 값을 그대로 표시 → 세션 시작·몹 등장 시 자동 갱신.
- 버튼은 **룸 트래킹 지원 기기에서만 활성화**. 시뮬레이터에서는 회색(=정상, 안전장치).

### `ImmersiveView.swift` — 무대

- `RealityView`로 매니저의 씬 루트를 화면에 올린다 → 매니저가 몹을 붙이면 바로 보인다.
- 화면이 뜰 때 `start()` 실행. 그 안의 업데이트 루프가 공간이 닫힐 때까지 돈다.

### `RoomDungeonManager.swift` — 핵심 로직

- Phase 1 로직 전부를 담는 클래스 (`@MainActor` + `@Observable`).
- `start()`: 지원 확인 → 세션 실행(do/catch) → 몹 1회 로드 → 방 업데이트 루프 진입.
- 루프: 이벤트가 제거면 해당 방 몹 정리, 추가/갱신이면 `isCurrentRoom`으로 spawn/despawn 판단.
- **몹을 띄운 방 id를 기억** → 방 이동 시 이벤트 순서가 뒤섞여도 항상 올바르게 교체.

### `Mob.swift` — 몹 만들기

- 번들의 `Turtle.usdz`를 이름으로 로드(비동기, 실패 시 nil).
- 모델 원본 단위(cm/m)가 제각각 → 가장 긴 변 기준 **약 0.3m로 정규화**(숫자 하나만 바꾸면 크기 조절).
- 몹 "만들기"와 "배치"를 분리 → 나중에 모델 교체 시 이 파일만 수정.

---

## ⭐ 가장 중요한 부분: 스폰 지점과 `contains()`

방 형태는 단순한 상자가 아니라 **복잡한 메시**로 온다. 그 안의 "적당한 한 점"을 얻는 게 관건.

이 코드는 메시를 직접 파싱하지 않고, **앵커 기준 위치 주변 높이 1.2m의 후보 점 몇 개**를 만들어
각각이 방 안인지 `contains()`로 확인한 뒤, **통과하는 첫 점**을 채택한다.

```swift
let candidates: [SIMD3<Float>] = [
    [0, 1.2, 0],
    [0.5, 1.2, 0], [-0.5, 1.2, 0],
    [0, 1.2, 0.5], [0, 1.2, -0.5],
]
for local in candidates {
    let p = transform * SIMD4<Float>(local.x, local.y, local.z, 1)
    let world = SIMD3<Float>(p.x, p.y, p.z)
    if room.contains(world) { return world }   // 방 안이면 채택
}
```

> ⚠️ `contains()` 검증은 단순 안전장치가 아니라, **룸 트래킹이 실제로 일하고 있다는 유일한
> 화면 증거**다. 검증 없이 아무 데나 몹을 띄우면 룸 트래킹이 필요 없어지므로 이 프로젝트의
> 존재 이유가 사라진다. 그래서 반드시 남긴다.

---

## 🚫 Phase 1에서 하지 않는 것

- 방마다 다른 몹 배정 (지금은 모든 방에 같은 거북이)
- 앱 재실행 후 방 기억 (영속성 없음)
- 전투 · 손 추적 · 무기
- 가상 벽 · 인공 배경

→ Phase 1은 **"방에 들어가면 몹이 떠 있다"** 한 가지에 집중.

---

## ✅ 확인 방법

| 환경 | 확인 가능한 것 |
|---|---|
| 시뮬레이터 | 빌드 통과, 창 UI, 입장 버튼 비활성(정상) |
| Vision Pro 실기기 | 방 안 몹 등장, 방 이동 시 사라졌다 재등장 |

> 방을 옮길 때 몹이 **사라졌다 다시 나타나는 장면**이 룸 트래킹 파이프라인 전체가 살아 있다는
> 증거다.
