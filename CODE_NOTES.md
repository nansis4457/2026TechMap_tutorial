# RoomDungeon — 코드 설명 노트 (Phase 1)

이 문서는 현재 구현된 코드를 **말로 설명하기 위한 정리**입니다. 발표나 리뷰에서
"이 코드가 무엇을, 왜, 어떻게 하는가"를 순서대로 이야기할 수 있도록 문장 중심으로 적었습니다.

---

## 1. 한 문장 요약

사용자가 몰입 공간(던전)에 들어가면, ARKit의 룸 트래킹으로 지금 서 있는 방을 감지하고,
그 방 안의 한 지점을 골라 거북이 몹을 띄웁니다. 방을 나가면 몹은 사라지고, 다시 들어오면
다시 나타납니다.

---

## 2. 전체 흐름 (데이터가 한 방향으로 흐른다)

이 앱의 핵심 원리는 "센서가 데이터를 밀어주면(push) 그걸 받아 화면을 바꾼다"입니다.
내가 매 순간 방 정보를 물어보는 게 아니라, 방이 바뀔 때마다 시스템이 알려주는 이벤트를
구독해서 반응합니다. 흐름은 다음 순서로 이어집니다.

먼저 사용자는 평면 창에서 "Enter Dungeon" 버튼을 눌러 몰입 공간에 들어갑니다. 룸 트래킹은
일반 창에서는 동작하지 않고 몰입 공간(Full Space) 안에서만 켜지기 때문입니다. 몰입 공간이
열리면 매니저가 ARKit 세션을 실행하고, 룸 트래킹 데이터 구독을 시작합니다. 이후 방에 대한
업데이트가 도착할 때마다 지금 서 있는 방인지 판별하고, 맞으면 그 방 안의 좌표를 하나 계산해
검증한 뒤 거북이를 그 자리에 붙입니다. 방을 벗어나면 같은 이벤트 흐름을 통해 거북이를 다시
떼어냅니다.

계층으로 나누면 이렇게 됩니다.

1. **무대(권한)** — 몰입 공간과 월드 센싱 권한
2. **런타임** — ARKit 세션과 룸 트래킹 구독권
3. **데이터 스트림** — 방 업데이트 이벤트를 하나씩 수신
4. **방 정체성** — "지금 이 방이 내가 있는 방인가"(isCurrentRoom)
5. **스폰 지점** — 방 안의 좌표를 골라 contains()로 검증
6. **생명주기** — 입장 시 스폰, 퇴장 시 디스폰

---

## 3. 파일별 설명

### RoomDungeonApp.swift — 앱의 뼈대

앱은 두 개의 화면(씬)으로 구성됩니다. 하나는 앱을 켜면 처음 보이는 평면 창이고, 다른 하나는
룸 트래킹이 실제로 도는 몰입 공간입니다. 몰입 공간은 앱을 켜자마자 자동으로 열 수 없고
사용자가 직접 들어가야 하기 때문에 이렇게 둘로 나눕니다.

두 화면은 같은 매니저 인스턴스를 공유해야 상태(세션, 몹)가 한 곳에서 관리됩니다. 그래서
매니저를 앱에서 한 번만 만들고 environment로 두 화면 모두에 주입합니다.

몰입 스타일은 `.mixed`를 씁니다. 몹이 "내 실제 방 안에" 떠 있는 것처럼 보이려면 패스스루로
실제 방이 보여야 하기 때문입니다. 화면을 완전히 가리는 `.full`이 아니라 `.mixed`인 이유가
여기에 있습니다.

### ContentView.swift — 입장 화면

평면 창으로, 제목과 현재 상태 문구, 그리고 입장/퇴장 버튼을 보여줍니다. 상태 문구는 매니저가
들고 있는 값을 그대로 표시하므로, 세션이 시작되거나 몹이 등장하면 문구가 자동으로 바뀝니다.

버튼을 누르면 몰입 공간을 열거나 닫는 비동기 작업이 실행됩니다. 공간을 여는 함수는 시간이
걸릴 수 있어 async이므로 Task로 감싸 호출합니다. 열기 결과가 성공인지, 사용자가 취소했는지,
실패했는지에 따라 현재 상태를 갱신합니다.

중요한 점은 버튼이 기기가 룸 트래킹을 지원할 때만 활성화된다는 것입니다. 시뮬레이터처럼
룸 트래킹이 없는 환경에서는 버튼이 비활성(회색)으로 표시됩니다. 이것은 버그가 아니라, 미지원
기기에서 무리하게 진행하지 않도록 막는 안전장치입니다.

### ImmersiveView.swift — 무대

몰입 공간 안에서 실제 3D 콘텐츠를 그리는 화면입니다. RealityView라는 컨테이너로 매니저가
소유한 씬 루트를 화면에 올립니다. 이후 매니저가 이 루트에 거북이를 붙이면 곧바로 화면에
나타납니다.

이 화면이 나타날 때 매니저의 `start()`를 실행합니다. `start()` 안에는 끝나지 않는 업데이트
루프가 들어 있어서, 몰입 공간이 열려 있는 동안 계속 방 이벤트를 받습니다. 공간이 닫히면 이
화면이 사라지면서 작업이 자동으로 취소되고 루프도 함께 끝납니다.

### RoomDungeonManager.swift — 핵심 로직

이 클래스가 Phase 1의 모든 로직을 담습니다. RealityKit과 UI를 안전하게 다루기 위해 메인
스레드에서만 동작하도록 지정했고(@MainActor), 상태가 바뀌면 화면이 자동으로 갱신되도록
관찰 가능하게(@Observable) 만들었습니다.

`start()`는 먼저 기기가 룸 트래킹을 지원하는지 확인하고, 지원하면 세션을 실행합니다. 세션
실행은 실패할 수 있는 비동기 작업이므로 do/catch로 감쌉니다. 예를 들어 권한 설명 문구가
없거나 사용자가 권한을 거부하면 여기서 오류가 나고, 그 내용을 상태 문구로 보여줍니다. 세션이
정상적으로 시작되면 거북이 모델을 한 번 로드해 둡니다. 몹은 화면에 항상 하나뿐이므로 매번 새로
만들 필요 없이, 한 번 로드해 두고 붙였다 뗐다 하며 재사용합니다.

그다음 방 업데이트 루프에 들어갑니다. 업데이트가 도착할 때마다 그 이벤트가 "앵커 제거"인지
확인하고, 제거된 방이 마침 몹을 띄운 방이라면 몹을 치웁니다. 제거가 아니라 추가/갱신이라면,
그 방이 지금 내가 서 있는 방인지(isCurrentRoom) 봅니다. 내가 있는 방인데 아직 그 방에 몹을
안 띄웠다면 새로 띄우고, 반대로 더 이상 내가 있는 방이 아닌데 거기에 몹이 있다면 치웁니다.

여기서 "몹을 띄운 방의 id"를 하나 기억해 두는 것이 핵심입니다. 방을 옮기면 이전 방과 새 방에
대한 이벤트가 순서가 뒤섞여 도착할 수 있는데, 이 id를 기준으로 판단하기 때문에 순서와 무관하게
항상 이전 몹을 치우고 새 방에 다시 띄우는 동작이 보장됩니다.

### Mob.swift — 몹 만들기

앱 번들에 들어 있는 Turtle.usdz 모델을 이름으로 로드합니다. 로드는 비동기이고 실패할 수 있어서,
실패하면 아무것도 반환하지 않습니다. 모델마다 원래 크기 단위가 제각각(센티미터 또는 미터)이라,
불러온 뒤 모델의 가장 긴 변을 재서 약 0.3미터가 되도록 크기를 맞춥니다. 실기기에서 너무 크거나
작으면 이 0.3이라는 숫자 하나만 조절하면 됩니다.

몹을 만드는 일과 몹을 어디에 놓을지 정하는 일을 분리해 둔 덕분에, 나중에 거북이를 다른 모델로
바꾸고 싶으면 이 파일만 손대면 되고 매니저의 로직은 건드릴 필요가 없습니다.

---

## 4. 가장 설명할 가치가 있는 부분: 스폰 지점과 contains()

몹을 어디에 놓을지 정하는 방식이 이 프로젝트에서 가장 중요한 대목입니다. 방의 형태 정보는
단순한 상자가 아니라 복잡한 메시로 주어지기 때문에, 그 안의 "적당한 한 점"을 얻는 것이
생각보다 까다롭습니다.

이 코드는 메시를 직접 뜯어보는 대신, 방 앵커의 기준 위치를 중심으로 높이 1.2미터쯤의 후보 점
몇 개(가운데와 그 주변)를 만들고, 각 후보가 실제로 방 안에 있는지 `contains()`로 하나씩
확인합니다. 통과하는 첫 번째 점을 몹 자리로 채택하고, 만약 후보가 전부 방 밖으로 판정되면
몹을 띄우지 않고 안내 문구만 남깁니다.

이 `contains()` 검증은 단순한 안전장치가 아니라, 룸 트래킹이 실제로 일하고 있다는 것을 화면으로
증명하는 유일한 장치입니다. 만약 검증 없이 아무 데나 몹을 띄운다면 룸 트래킹이 없어도 되는
것이므로, 이 프로젝트의 존재 이유 자체가 사라집니다. 그래서 이 부분은 반드시 남겨 둡니다.

---

## 5. 이 코드가 하지 않는 것 (Phase 1 범위 밖)

의도적으로 넣지 않은 것들이 있습니다. 방마다 다른 몹을 배정하는 기능은 없고, 지금은 모든 방에
같은 거북이가 나옵니다. 앱을 껐다 켜도 방을 기억하는 영속성 기능도 없습니다. 전투, 손 추적,
무기 같은 것도 없습니다. 가상의 벽을 세우거나 인공적인 배경을 만들지도 않습니다. 이 모든 것은
이후 단계의 몫이며, Phase 1은 "아무 방에나 들어가면 그 방 안에 몹이 떠 있다"는 한 가지 목표에
집중합니다.

---

## 6. 어떻게 확인하나

시뮬레이터에서는 룸 트래킹이 없으므로 입장 버튼이 비활성으로 보이는 것까지가 정상이며, 여기서
확인할 수 있는 것은 빌드 통과 여부와 창 UI 정도입니다. 실제 동작은 Apple Vision Pro 실기기에서만
확인할 수 있습니다. 실기기에서 몰입 공간에 들어가 방 안을 둘러보면 거북이가 떠 있어야 하고,
다른 방으로 이동하면 이전 거북이가 사라졌다가 새 방에서 다시 나타나야 합니다. 이 "사라졌다
다시 나타나는" 장면이 룸 트래킹 파이프라인 전체가 살아 있다는 증거입니다.
